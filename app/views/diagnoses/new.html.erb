<div class="pt-10">
  <%= form_with url: diagnosis_result_path, method: :post, local: true, data: { turbo: false }, html: { class: "max-w-2xl mx-auto bg-white p-8 rounded shadow-md" }  do |f| %>
    <% [
      "優しい目元で、共感力が高く人の気持ちに寄り添える。",
      "清潔感のある服装で、気配り上手な性格。",
      "ファッションに気を配りつつ、言葉に敏感で思いやりがある。"
    ].each_with_index do |desc, i| %>
      <div class="flex items-start space-x-4 mb-4 ">
        <%= f.radio_button :answer, i, id: "answer_#{i}", class: "radio radio-lg radio-accent mt-1" %>
        <%= f.label :answer, desc, for: "answer_#{i}", class: "text-lg leading-relaxed" %>
      </div>
    <% end %>

    <div class="mt-6 text-center">
      <%= f.submit "診断結果へ", class: "btn bg-[#D5A273] text-white hover:bg-[#b77f53] px-6 text-lg" %>
    </div>
  <% end %>
</div>